import { Routes } from '@angular/router';
import { ListDataComponent } from './list-data.component';

export const ListDataRoutes: Routes = [
    {
        path: '',
        component: ListDataComponent
    }
];